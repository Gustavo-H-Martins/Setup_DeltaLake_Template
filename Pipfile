[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
deltalake = ">=0.15.1"
pandas = ">=2.2.0"
pyspark = ">=3.5.0"
polars = ">=0.20.5"
delta-spark = ">=3.0.0"
duckdb = ">=0.9.2"

[dev-packages]
ipykernel = "*"

[requires]
python_version = "3.11"
